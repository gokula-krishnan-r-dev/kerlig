import SwiftUI

struct SettingsView: View {
    var body: some View {
        VStack {
            TabFeature()

            Divider()

        
        }
    }
}

#Preview {
    SettingsView()
        .frame(width: 800, height:600)
}
